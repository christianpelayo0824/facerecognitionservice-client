mainApp.controller('ProfileController', function ($scope, $location) {
    // var url = window.location;
    // var element = $('ul#sidebarnav a').filter(function () {
    //     return this.href == url;
    // }).addClass('active').parent().addClass('active');
});